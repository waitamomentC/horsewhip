# 本项目 · Claude Code

> **仅用于还没有 `CLAUDE.md` 的新项目。**  
> 已有 `CLAUDE.md` 时请用 [`protocol/docs/claude-code.md`](../docs/claude-code.md) **②** 流程，不要覆盖。

## Horsewhip（自动加载）

- `.claude/rules/horsewhip-protocol.md`（= 根目录 `AGENTS.md`）
- 冲突：Git/边界 → Horsewhip；构建/架构 → 本文件

---

## 本项目说明（团队填写）

### 构建与测试

```bash
# npm install && npm test
```

### 目录与约定

```text

```
